@model List<Chartify.ViewModels.ChartViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<div class="col">
    <a asp-action="Create" class="btn btn-info w-50">Create a new Chart</a>
</div>

<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.DifficultyName)
                </td>
                <td>
                    <a asp-action="Update" asp-controller="Chart" asp-route-id="@item.Id">Update</a> |
                    <a asp-action="Details" asp-controller="Chart" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-controller="Chart" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>